export module Example:Layers;

// values used in the layers & masks for the collisions detection
export enum Layers {
    NONE        = 0b0000,
    WORLD       = 0b0001,
    BODIES      = 0b0010,
    PLAYER      = 0b0100,
};

